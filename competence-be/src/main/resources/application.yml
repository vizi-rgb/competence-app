spring:
  application:
    name: Competence
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui.html