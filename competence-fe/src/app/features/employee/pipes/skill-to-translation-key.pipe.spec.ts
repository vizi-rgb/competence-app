import { SkillToTranslationKeyPipe } from './skill-to-translation-key.pipe';

describe('SkillToTranslationKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillToTranslationKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
